# 📊 SYSTEM ARCHITECTURE DIAGRAMS

## Interior Design Project Management System

---

## 🏗️ SYSTEM ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────┐
│                         USER LAYER                          │
│  ┌─────────────────┐              ┌─────────────────┐      │
│  │  Web Browser    │              │  Web Browser    │      │
│  │  (Designer)     │              │  (Client)       │      │
│  └────────┬────────┘              └────────┬────────┘      │
└───────────┼──────────────────────────────────┼─────────────┘
            │                                  │
            ↓                                  ↓
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │           Streamlit Frontend (app.py)                  │ │
│  │  - Login/Logout Interface                              │ │
│  │  - Session Management                                  │ │
│  │  - Dashboard Routing                                   │ │
│  └────────┬───────────────────────────────────┬───────────┘ │
└───────────┼───────────────────────────────────┼─────────────┘
            │                                   │
            ↓                                   ↓
    ┌───────────────┐                  ┌───────────────┐
    │   Designer    │                  │    Client     │
    │  Dashboard    │                  │  Dashboard    │
    └───────┬───────┘                  └───────┬───────┘
            │                                  │
            ↓                                  ↓
┌─────────────────────────────────────────────────────────────┐
│                     BUSINESS LOGIC LAYER                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   auth.py    │  │  utils.py    │  │  config.py   │      │
│  │ Authentication│  │   Helpers    │  │   Settings   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Feature Modules                           │ │
│  │  - designer_budget.py                                  │ │
│  │  - designer_suppliers.py                               │ │
│  │  - designer_measurements.py                            │ │
│  │  - designer_timeline.py                                │ │
│  │  - designer_feedback.py                                │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      DATA ACCESS LAYER                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              database.py                               │ │
│  │  - Connection Manager                                  │ │
│  │  - CRUD Operations                                     │ │
│  │  - Query Execution                                     │ │
│  │  - Context Managers                                    │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                       DATABASE LAYER                         │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              MySQL Server                              │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │ │
│  │  │  users   │  │ projects │  │  tasks   │             │ │
│  │  └──────────┘  └──────────┘  └──────────┘             │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │ │
│  │  │  budget  │  │suppliers │  │ timeline │             │ │
│  │  └──────────┘  └──────────┘  └──────────┘             │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │ │
│  │  │ feedback │  │ gallery  │  │reference │             │ │
│  │  └──────────┘  └──────────┘  └──────────┘             │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      FILE STORAGE LAYER                      │
│  ┌────────────────────────────────────────────────────────┐ │
│  │           Local File System (uploads/)                 │ │
│  │  projects/                                             │ │
│  │    └── {project_id}/                                   │ │
│  │         ├── reference/                                 │ │
│  │         ├── drawings/                                  │ │
│  │         ├── gallery/                                   │ │
│  │         └── whiteboard/                                │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 AUTHENTICATION FLOW

```
┌─────────────┐
│    User     │
└──────┬──────┘
       │
       ↓
┌──────────────────┐
│   Login Page     │  Designer or Client?
└────┬────────┬────┘
     │        │
     ↓        ↓
┌─────────┐ ┌─────────────┐
│Designer │ │   Client    │
│ Email + │ │  Email +    │
│Password │ │Access Code  │
└────┬────┘ └──────┬──────┘
     │             │
     ↓             ↓
┌────────────────────────────┐
│  auth.py                   │
│  - Verify credentials      │
│  - Hash password (designer)│
│  - Check database          │
└──────────┬─────────────────┘
           │
    ┌──────┴──────┐
    │ Valid?      │
    └──┬─────┬────┘
       │     │
      Yes    No
       │     │
       ↓     ↓
┌──────────┐ ┌──────────┐
│ Create   │ │  Error   │
│ Session  │ │ Message  │
└────┬─────┘ └──────────┘
     │
     ↓
┌──────────────┐
│ Save Cookie  │
│ (optional)   │
└──────┬───────┘
       │
       ↓
┌──────────────┐
│  Dashboard   │
│  (role-based)│
└──────────────┘
```

---

## 📊 DATABASE ENTITY RELATIONSHIPS

```
┌──────────────────┐
│      USERS       │
│ ──────────────── │
│ PK: id           │
│    name          │
│    email         │
│    password_hash │ (NULL for clients)
│    role          │ (designer/client)
│    client_code   │ (NULL for designers)
└────┬────────┬────┘
     │        │
     │        │ (designer_id)
     │        ↓
     │   ┌──────────────────┐
     │   │    PROJECTS      │
     │   │ ──────────────── │
     │   │ PK: id           │
     │   │ FK: client_id    │─────────┐
     │   │ FK: designer_id  │         │
     │   │    site_type     │         │
     │   └────┬─────────────┘         │
     │        │                       │
     └────────┘ (client_id)           │
              │                       │
              │ (project_id FK in all below)
              │
     ┌────────┼─────────┬─────────┬─────────┐
     │        │         │         │         │
     ↓        ↓         ↓         ↓         ↓
┌─────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐
│reference│ │ tasks  │ │ budget │ │timeline│ │ feedback │
│ library │ │        │ │ items  │ │        │ │          │
└─────────┘ └────────┘ └────────┘ └────────┘ └──────────┘
     ↓         ↓         ↓          ↓          ↓
┌─────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐
│measurements│gallery│ │ notes_ │ │        │ │          │
│           │        │ │whiteboard│        │ │          │
└───────────┘└────────┘ └────────┘ └────────┘ └──────────┘

┌──────────────┐
│  SUPPLIERS   │ (Independent - no FK)
│ ──────────── │
│ PK: id       │
│    name      │
│    category  │
│    contact   │
└──────────────┘
```

---

## 🔄 DATA FLOW: CREATE PROJECT

```
Designer (Browser)
     │
     ↓ 1. Fill form
┌─────────────────────┐
│ Project Management  │
│ designer_dashboard.py│
└─────────┬───────────┘
          │
          ↓ 2. Submit
┌─────────────────────┐
│ register_client()   │
│ auth.py             │
│ - Generate code     │
│ - Hash (if needed)  │
└─────────┬───────────┘
          │
          ↓ 3. Insert client
┌─────────────────────┐
│ insert_record()     │
│ database.py         │
└─────────┬───────────┘
          │
          ↓ 4. Save to DB
┌─────────────────────┐
│ MySQL - users table │
└─────────┬───────────┘
          │
          ↓ 5. Get client_id
┌─────────────────────┐
│ Create project      │
│ insert_record()     │
└─────────┬───────────┘
          │
          ↓ 6. Save project
┌─────────────────────┐
│ MySQL - projects    │
└─────────┬───────────┘
          │
          ↓ 7. Get project_id
┌─────────────────────┐
│ Initialize defaults │
│ - Tasks             │
│ - Budget categories │
└─────────┬───────────┘
          │
          ↓ 8. Create folders
┌─────────────────────┐
│ File System         │
│ uploads/projects/   │
│   {project_id}/     │
└─────────┬───────────┘
          │
          ↓ 9. Return success
┌─────────────────────┐
│ Display client code │
│ Show success message│
└─────────────────────┘
```

---

## 📤 FILE UPLOAD FLOW

```
User uploads file
     │
     ↓
┌─────────────────────┐
│ st.file_uploader()  │
│ Streamlit widget    │
└─────────┬───────────┘
          │
          ↓
┌─────────────────────┐
│ save_uploaded_file()│
│ utils.py            │
│ 1. Get project_id   │
│ 2. Determine type   │
│ 3. Create directory │
└─────────┬───────────┘
          │
          ↓
┌─────────────────────┐
│ Generate filename   │
│ timestamp + name    │
└─────────┬───────────┘
          │
          ↓
┌─────────────────────┐
│ Write to disk       │
│ uploads/projects/   │
│   {id}/{type}/      │
└─────────┬───────────┘
          │
          ↓
┌─────────────────────┐
│ Return relative path│
│ e.g., "projects/    │
│  1/reference/       │
│  20260129_img.jpg"  │
└─────────┬───────────┘
          │
          ↓
┌─────────────────────┐
│ insert_record()     │
│ Store path in DB    │
│ (reference_library, │
│  gallery, etc.)     │
└─────────┬───────────┘
          │
          ↓
┌─────────────────────┐
│ Success!            │
│ File available for  │
│ viewing/download    │
└─────────────────────┘
```

---

## 🎯 USER INTERACTION FLOW

### Designer Workflow
```
1. Sign Up/Login
   ↓
2. Project Management
   - Create client
   - Note client code
   ↓
3. Select Active Project
   ↓
4. Work on Features:
   - Upload references
   - Add/update tasks
   - Manage budget
   - Upload drawings
   - Set timeline
   - View feedback
   ↓
5. Monitor Progress
   - Check task completion
   - Review budget status
   - Read client feedback
```

### Client Workflow
```
1. Receive email + code from designer
   ↓
2. Login with code
   ↓
3. View Project Information:
   - Reference images
   - Task progress
   - Budget breakdown
   - Drawings
   - Timeline
   ↓
4. Upload Inspiration (Gallery)
   ↓
5. Provide Feedback
   - Comment on designs
   - Approve/reject items
   ↓
6. Track Progress
   - Monitor completion %
   - Check milestones
```

---

## 🧩 MODULE INTERACTIONS

```
app.py (Main Entry)
  │
  ├──→ config.py (Settings)
  │
  ├──→ database.py (Data Operations)
  │     └──→ MySQL Server
  │
  ├──→ auth.py (Authentication)
  │     ├──→ bcrypt (Hashing)
  │     ├──→ database.py
  │     └──→ extra_streamlit_components (Cookies)
  │
  ├──→ utils.py (Helpers)
  │     ├──→ Pillow (Images)
  │     ├──→ config.py
  │     └──→ File System
  │
  ├──→ designer_dashboard.py
  │     ├──→ auth.py
  │     ├──→ database.py
  │     ├──→ utils.py
  │     ├──→ designer_budget.py
  │     ├──→ designer_suppliers.py
  │     ├──→ designer_measurements.py
  │     ├──→ designer_timeline.py
  │     └──→ designer_feedback.py
  │
  └──→ client_dashboard.py
        ├──→ auth.py
        ├──→ database.py
        └──→ utils.py
```

---

## 📊 SESSION STATE MANAGEMENT

```
User Login Success
     │
     ↓
┌─────────────────────────┐
│ st.session_state        │
│ ─────────────────────── │
│ logged_in: True         │
│ user_id: 123            │
│ user_name: "John"       │
│ user_email: "j@e.com"   │
│ user_role: "designer"   │
│                         │
│ (Designer only)         │
│ active_project_id: 1    │
│ active_project_name: "" │
│                         │
│ (Client only)           │
│ client_code: "ABC123"   │
│ client_project_id: 1    │
└─────────────────────────┘
         │
         ↓
┌─────────────────────────┐
│ Cookie (Optional)       │
│ ─────────────────────── │
│ Name: interior_design_  │
│       session           │
│ Value: "123:designer:   │
│         email@test.com" │
│ Expiry: 30 days         │
└─────────────────────────┘
```

---

**Architecture Documentation Complete! 🎉**
